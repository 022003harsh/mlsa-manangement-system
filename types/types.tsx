export type SideNavItem = {
  name: string;
  path: string;
  iconURL?: string;
};

export type MembersDataItem = {
  name: string;
  path: string;
  iconURL?: string;
  position: string;
};

export type ModalItem = {
  value: string;
  topic: string;
  description: string;
};